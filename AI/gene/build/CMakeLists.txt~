SET(ROOT_DIR ${PROJECT_SOURCE_DIR}/..)
SET(SOURCE_DIR ${ROOT_DIR}/src)
SET(INCLUDE_DIR ${ROOT_DIR}/include)

SET(CMAKE_RUNTIME_OUTPUT_DIRECTORY ${ROOT_DIR}/buid)
SET(PROJECT_LIBRARY_PATH ${ROOT_DIR}/build)
SET(LIBRARY_OUTPUT_PATH ${ROOT_DIR}/build)


SET(source_file ${SOURCE_DIR}/population.cpp)
INCLUDE_DIRECTORIES(${INCLUDE_DIR})

ADD_LIBRARY(demo ${source_file})
